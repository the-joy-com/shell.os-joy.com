{
  "name": "shell-os-joy",
  "private": true,
  "version": "0.0.6",
  "type": "module",
  "license": "MIT",
  "author": "yactouat",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.sw.json && vite build && vite build --config vite.sw.config.ts",
    "preview": "vite preview",
    "serve": "yarn build && yarn preview"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "typescript": "^6.0.0",
    "vite": "^8.0.0"
  }
}
